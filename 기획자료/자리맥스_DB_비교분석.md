# 자리맥스(ZARIMAX) 데이터베이스 비교분석 보고서

> **작성일**: 2026-02-08  
> **대상 서비스**: 자리맥스 - AI 기반 상업용 부동산 매칭 플랫폼  
> **기술 스택**: Flutter 3.x + FastAPI + PostgreSQL 16 + PostGIS + GPT-4o-mini

---

## 1. 데이터베이스 후보군 비교 (4종)

자리맥스의 핵심 요구사항(지도 기반 위치 서비스, 공간 쿼리, AI 매칭)을 기준으로 4가지 DB를 비교 분석했다.

### 1.1 종합 평가

| 항목 | Supabase (PostgreSQL) | PostgreSQL (자체 호스팅) | MongoDB Atlas | Firebase |
|---|---|---|---|---|
| **추천 순위** | 🥇 **1위 (MVP 최적)** | 🥈 2위 (스케일) | 🥉 3위 | 4위 (부적합) |
| PostGIS 공간쿼리 | ✅ 기본 내장 | ✅ 직접 설치 | ⚠️ $nearSphere 제한적 | ❌ 미지원 |
| Flutter SDK | ✅ supabase_flutter | ❌ 직접 구현 | ✅ realm_dart | ✅ FlutterFire |
| 실시간 기능 | ✅ Realtime 내장 | ❌ 별도 구축 필요 | ✅ Change Streams | ✅ Firestore |
| 인증 시스템 | ✅ Supabase Auth | ❌ 별도 구축 | ❌ 별도 구축 | ✅ Firebase Auth |
| 비용 (MVP) | $0~25/월 | VPS $5~50/월 | $0~57/월 | $0 (Spark) |
| 관리 부담 | 낮음 (완전 관리형) | 높음 (직접 관리) | 중간 | 낮음 |

### 1.2 후보별 상세 평가

**Supabase (1위)**: PostGIS 내장으로 공간 쿼리 즉시 사용 가능하며, Flutter SDK(supabase_flutter)와 Auth, Storage, Realtime, REST API가 모두 포함된 올인원 솔루션이다. 예측 가능한 월 비용($25/월 Pro)으로 MVP 개발 속도가 가장 빠르다.

**PostgreSQL 자체 호스팅 (2위)**: 100% 무료 오픈소스이며 기능 제한이 전혀 없다. PostGIS, 파티셔닝, 복제 등 모든 기능을 제한 없이 사용할 수 있다. 단, Auth/API/Pooler/백업/보안을 모두 직접 구축해야 하므로 초기 개발 비용이 크다. 대규모 스케일(100K+ DAU) 시 최종 마이그레이션 대상으로 적합하다.

**MongoDB Atlas (3위)**: 유연한 스키마가 장점이나, 지도 기반 앱에 필수적인 공간 쿼리가 PostGIS 대비 제한적이다. 자리맥스의 상권분석, 반경 검색, 행정구역 매칭 같은 복합 공간 연산에는 부적합하다.

**Firebase (4위)**: Firestore는 GeoPoint 기반 반경 검색만 가능하며, 복합 공간 쿼리(폴리곤 교차, 행정구역 내 검색 등)를 지원하지 않아 지도 중심 앱에는 근본적으로 부적합하다.

---

## 2. PostgreSQL 무료 vs 유료 구조

PostgreSQL 자체는 상용/비상용 구분 없이 **완전 무료 오픈소스**이다. "Free 플랜"이나 "Pro 플랜" 같은 개념이 존재하지 않으며, max_connections 설정도 소프트웨어 제한 없이 하드웨어 성능에 따라 자유롭게 조절 가능하다.

비용이 발생하는 부분은 **호스팅 인프라**이다.

| 운영 방식 | 월 비용 | 동시접속 한계 | 관리 부담 |
|---|---|---|---|
| 직접 VPS 설치 | $5~50 (서버비만) | 하드웨어에 따라 자유 | 백업/보안/업데이트 전부 직접 |
| AWS RDS PostgreSQL | ~$15 (db.t3.micro) | 인스턴스 사양에 따라 | AWS 관리형 |
| Google Cloud SQL | ~$7 (db-f1-micro) | 인스턴스 사양에 따라 | GCP 관리형 |
| fly.io PostgreSQL | $0~29+ | 사양에 따라 | 반자동 관리 |
| **Supabase** | **$0~25+** | **플랜별 제한 (60~100+)** | **완전 관리형** |

핵심 차이: Supabase의 "Free vs Pro" 동시접속 제한은 PostgreSQL의 제한이 아니라 **Supabase가 컴퓨트 인스턴스 사양별로 설정한 인프라 제한**이다.

### 2.1 PostgreSQL 직접 운영 시 동시접속 처리

RAM 1GB VPS에 PostgreSQL을 직접 설치하면 Supabase Micro(Pro)와 동일한 하드웨어에서도 max_connections를 200~300으로 설정할 수 있다. 다만 실제 활용을 위해서는 PgBouncer 같은 커넥션 풀러를 직접 설치·운영해야 한다.

실무 가이드라인: RAM 1GB당 100~150 direct connections가 안정적이며, PgBouncer Transaction 모드 적용 시 클라이언트 수천 개 수용 가능하다.

---

## 3. Supabase Free vs Pro 동시접속 상세 비교

Supabase의 "동시접속"은 **3가지 레이어**로 구분된다.

### 3.1 Layer 1: DB 직접 연결 (Direct Connections = max_connections)

| 항목 | Free (Nano) | Pro (Micro) |
|---|---|---|
| **메모리** | ~0.5 GB | 1 GB |
| **max_connections** | ~60개 | ~100개 |
| **CPU** | Shared | 2-core ARM (shared) |

Nano 인스턴스의 Pool Size(실제 DB 백엔드 연결)는 약 15~20개이며, max_connections 기본값은 60이다. Pro Micro는 메모리가 2배이므로 약 100개까지 늘어난다.

### 3.2 Layer 2: Connection Pooler (Supavisor) 클라이언트 연결

| 항목 | Free (Nano) | Pro (Micro) |
|---|---|---|
| **Max Client Connections** | ~200 | ~1,500 (추정) |
| **Pool Size (백엔드)** | ~15 | ~30 |
| **Dedicated Pooler (PgBouncer)** | ❌ 미제공 | ✅ 제공 |

Supavisor의 연결 제한은 하드코딩되어 있어 컴퓨트 사이즈 업그레이드 없이는 변경 불가능하다. Pro에서 제공되는 Dedicated Pooler(PgBouncer)는 DB와 동일 서버에 위치하여 최적의 성능과 지연 시간을 보장한다.

**Transaction Mode vs Session Mode**:
- **Transaction Mode (포트 6543)**: 연결을 효율적으로 공유하며, 서버리스/고동시성 환경에서 필수
- **Session Mode (포트 5432)**: 1:1 클라이언트-백엔드 매핑으로 연결이 빠르게 소진됨

### 3.3 Layer 3: Realtime WebSocket 동시접속

| 항목 | Free | Pro |
|---|---|---|
| **Realtime 동시접속** | 200개 | 500개 (기본 포함) |
| **초과 비용** | 불가 (제한 도달 시 차단) | $10 / 1,000 피크 연결 |
| **메시지** | 200만/월 | 500만/월 (초과: $2.50/100만) |

### 3.4 기타 주요 차이점

| 항목 | Free | Pro |
|---|---|---|
| **비활성 시 자동 일시중지** | ✅ 1주 비활성 시 pause | ❌ 일시중지 없음 |
| **프로젝트 수** | 최대 2개 | 무제한 |
| **DB 용량** | 500 MB | 8 GB |
| **월 비용** | $0 | $25 + 사용량 |
| **Compute 크레딧** | 없음 | $10/월 포함 |

---

## 4. 자리맥스 구현을 위한 기술 가이드

### 4.1 FastAPI + SQLAlchemy Transaction Mode 설정

```python
# FastAPI + SQLAlchemy 설정 (Transaction Mode 필수)
from sqlalchemy.ext.asyncio import create_async_engine
from sqlalchemy.pool import NullPool

DATABASE_URL = "postgresql+asyncpg://user:pass@host.supabase.co:6543/postgres"

engine = create_async_engine(
    DATABASE_URL,
    poolclass=NullPool,  # Supavisor가 커넥션 관리
    connect_args={
        "statement_cache_size": 0,        # Prepared statements 비활성화
        "prepared_statement_cache_size": 0
    }
)
```

**핵심 설정 사항**:
- Transaction Mode(포트 6543) 사용 필수
- NullPool 설정으로 Supavisor에게 커넥션 관리 위임
- `statement_cache_size: 0`으로 Prepared statements 비활성화 (Transaction Mode 호환)

### 4.2 Connection Pool 할당 가이드

- PostgREST API 사용 시: Pool Size ≤ max_connections의 40%
- PostgREST 미사용 시: Pool Size를 max_connections의 80%까지 사용 가능
- pg_stat_activity로 실시간 연결 분석 필요
- Nano 인스턴스 기준: 60개 max_connections 중 15~20개 Pool Size가 일반적

### 4.3 모니터링 필수 항목

- Grafana Dashboard 활성화 (200+ 메트릭 제공)
- "Disk IO % consumed" 지표 모니터링 (>1% 시 베이스라인 초과)
- pg_stat_activity로 실시간 연결 상태 분석
- "Max client connections reached" 에러 로그 추적
- Connection Pool 소진 알림 설정

---

## 5. 스케일링 전략 및 비용 로드맵

### 5.1 단계별 성장 경로

| 단계 | 인프라 | 월 비용 | 예상 DAU | 비고 |
|---|---|---|---|---|
| **MVP 출시** | Supabase Pro (Micro) | ~$25 | 100~1,000 | $10 Compute 크레딧 포함 (실 $15) |
| **성장기** | Supabase Small/Medium | $40~85 | 1,000~10,000 | 컴퓨트 사이즈만 업그레이드 |
| **확장기** | Supabase Large+ | $110+ | 10,000~100,000 | |
| **엔터프라이즈** | Self-hosted PostgreSQL | 인프라 비용 | 100,000+ | 완전 자체 관리로 전환 |

### 5.2 비용 최적화 포인트

- Pro 플랜 $10 Compute 크레딧으로 Micro 인스턴스 실질 비용 $15/월
- Realtime 500 연결 기본 포함 (지도 앱은 대부분 초과하지 않음)
- DB 용량 8 GB 기본 포함 (MVP 텍스트/JSON 데이터에 충분)
- Egress 250 GB 기본 포함 (API 응답에 적합)
- Redis 캐싱 도입으로 DB 쿼리 부하 경감 → 커넥션 용량 확장 효과

### 5.3 마이그레이션 경로

```
Supabase Free (개발/테스트)
    ↓
Supabase Pro Micro ($25/월) - MVP 출시
    ↓  
Supabase Pro Medium ($85/월) - 성장기
    ↓
Self-hosted PostgreSQL + PgBouncer - 100K+ DAU 시 전환
```

---

## 6. 자리맥스 아키텍처와의 통합 포인트

| PRD 기술 스택 | DB 연동 방식 | 비고 |
|---|---|---|
| Flutter 멀티플랫폼 | Transaction Mode Pooler 사용 | supabase_flutter SDK 활용 |
| FastAPI 백엔드 | NullPool + Prepared statements 비활성화 | 위 코드 예제 참조 |
| PostgreSQL 16 + PostGIS | Supabase에서 기본 지원 | 모든 Compute 사이즈에서 사용 가능 |
| 카카오맵 SDK | 클라이언트 사이드 처리 | DB 연결 영향 없음 |
| GPT-4o-mini AI 서버 | 별도 서비스로 운영 | 최소한의 DB 연결 사용 |
| Redis 캐싱 | DB 쿼리 부하 경감 | 커넥션 용량 확보 효과 |

---

## 7. 최종 권장사항

### Free 플랜의 한계 (프로덕션 부적합)

- DB 직접 연결 60개 + Pooler 200개로 동시 200명 이상 지도 조회 시 연결 오류 발생
- 1주 비활성 시 자동 일시중지로 서비스 가용성 보장 불가
- 500 MB DB 용량으로 상권 데이터 저장에 한계
- 개발/테스트 목적으로만 사용 권장

### Pro 플랜 권장 (MVP 최적)

- $25/월(실질 $15/월)로 프로덕션 운영 가능
- Dedicated Pooler로 안정적 커넥션 관리
- 자동 일시중지 없음 → 24/7 서비스 가용
- 8 GB DB + PostGIS로 MVP 상권 데이터 충분
- 트래픽 증가 시 컴퓨트 사이즈만 업그레이드하면 됨

### 장기 전략

100K+ DAU 도달 시 Self-hosted PostgreSQL + PgBouncer로 전환하여 인프라 제어권 확보 및 비용 최적화를 추진한다. Supabase의 PostgreSQL 기반이므로 마이그레이션 호환성이 높다.

---

> **참고 출처**: Supabase 공식 문서(supabase.com/docs), Supabase Connection Management 가이드, Supabase Realtime Pricing 문서, Supadex 가격 분석 블로그, Medium FastAPI+Supabase 연결 스케일링 가이드
